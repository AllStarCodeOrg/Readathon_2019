<!DOCTYPE html>
<html>
<%include ../partials/head%>
<body>
        <link href="https://fonts.googleapis.com/css?family=Amatic+SC|Great+Vibes" rel="stylesheet">
        <style>
            body{
                background-color: #1f2022;
            }

            h1{
                color: var(--primaryLight);
                font-family: var(--bodyFont)!important;
            }
        </style>
        <div class="main-container d-flex vh-100 flex-column">
                <nav class="row no-gutters bg-primaryDark">
                    <a href="/logout" class="col d-flex justify-content-center align-items-center text-center m-4 text-ascBlue h2">
                        Logout
                    </a>
                </nav>
            <div class="content-container d-flex flex-center flex-column text-center">
                <%if(locals.msg){%>
                    <%if(locals.msg.error){%>
                        <h1>
                            <%=locals.msg.error%>
                        </h1>
                        <%}else{%>
                            <h1 style="font-family: 'Amatic SC', cursive;">
                                Whoops, something went amiss!
                            </h1>
                            <%}%>
                            <%}else{%>
                                <h1 style="font-family: 'Amatic SC', cursive;">
                                    Whoops, something went amiss!
                                </h1>
                        <%}%>
                <img width="400px" src="/static/images/no_pass.gif" alt="">
            </div>
<%include ../partials/footer%>
</div>
</body>
</html>